<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowLightStatusBar"
            tools:targetApi="m">true</item>
        <item name="android:statusBarColor">#ffffff</item>


    </style>
    <style name="semiBold" parent="@android:style/TextAppearance">
        <item name="fontFamily">@font/seguisb</item>
        <item name="android:textColor">#5A5959</item>
    </style>

    <style name="latoBold" parent="@android:style/TextAppearance">
        <item name="fontFamily">@font/lato_bold</item>
        <item name="android:textColor">@android:color/transparent</item>
        <item name="android:textSize">26sp</item>
        <item name="android:elevation">2dp</item>
    </style>

    <style name="latoBoldGreen" parent="@android:style/TextAppearance">
        <item name="fontFamily">@font/lato_bold</item>
        <item name="android:textColor">#17801C</item>
        <item name="android:textSize">18sp</item>
        <item name="android:elevation">2dp</item>
    </style>


</resources>
